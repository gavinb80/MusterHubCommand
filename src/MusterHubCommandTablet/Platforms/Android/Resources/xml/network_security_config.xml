<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext only to local development addresses, same config
         as the main app's. Without this, Android blocks plain http://
         entirely from API 28+, which is what AppConfig.ApiBaseUrl's DEBUG
         default (http://10.0.2.2:5188 on the emulator) needs. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="true">local</domain>
    </domain-config>
    <!-- All other traffic (prod: musterhub-command.azurewebsites.net) must use HTTPS -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
